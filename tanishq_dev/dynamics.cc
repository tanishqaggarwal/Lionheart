#include <array>
#include <cmath>

/// @brief Implements 6DOF dynamics for rover.
class Rover
{
private:
    // Internal state of system

    /// Depth of the CG.
    double cg_depth;

    /// Positions of thrust vectors
    

public:
};
